apiVersion: 1

datasources:
  - uid: prometheus
    name: Prometheus
    type: prometheus
    access: proxy
    # Grafana 12 provisioning in this setup was resolving the env-substituted
    # URL to an empty string, so keep the internal compose DNS name explicit.
    url: http://prometheus:9090
    isDefault: true
    editable: true
